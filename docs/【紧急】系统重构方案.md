# AutoMoney 系统架构重构方案

## 🚨 当前系统严重问题诊断

### 问题1: 网络连接失败导致系统瘫痪 (P0)
```
RuntimeError: 无法获取真实市场数据
httpx.ConnectError (TLS handshake失败)
```

**根本原因**:
- 调度器对实时数据**强依赖**
- 一个数据源失败 → 整个策略执行中断
- 没有降级机制、缓存机制、重试机制

**影响**:
- ❌ 两个策略模板都无法自动执行
- ❌ 系统完全不可用

### 问题2: Agent执行逻辑混乱 (P0)
```
旧策略: business_agents = ['macro', 'ta', 'onchain']
动量策略: business_agents = ['regime_filter', 'ta_momentum']
```

**架构问题**:
1. **两套Agent执行器并存**:
   - `RealAgentExecutor`: 硬编码执行3个旧Agent
   - `DynamicAgentExecutor`: 动态执行新Agent
   - 两者逻辑不一致,维护成本高

2. **Agent注册机制混乱**:
   - 数据库有`agent_registry`表,但未被使用
   - Agent实例化散落在各处(registry.py, executor, orchestrator)
   - 新旧Agent混杂,无法统一管理

3. **执行记录不统一**:
   - `record_macro_agent`, `record_ta_agent`, `record_onchain_agent` (硬编码)
   - `record_generic_agent` (通用方法)
   - 两套记录逻辑并存

### 问题3: 数据采集架构脆弱 (P1)
```
DataCollectionManager
  → BinanceCollector (失败) → 整个系统崩溃
  → FREDCollector
  → AlternativeMeCollector
  ...
```

**架构问题**:
- 无容错机制
- 无降级策略
- 无缓存机制
- 无mock数据支持

### 问题4: UI与Backend状态不同步 (P1)
- UI显示旧Agent
- Backend记录新Agent执行
- 前后端数据不一致

---

## 🎯 重构目标

### 核心原则
1. **高可用优于高精度**: 系统能降级运行,不能因单点故障瘫痪
2. **配置驱动**: Agent组合、数据源、执行逻辑完全由配置定义
3. **统一抽象**: 一套Agent执行器,一套记录逻辑,一套数据采集
4. **解耦合**: 数据层、Agent层、决策层、交易层互不依赖

### 重构范围
- ✅ **保留**: 数据库schema、决策Agent、UI组件
- ♻️ **重构**: Agent执行器、数据采集、调度器
- ❌ **废弃**: `RealAgentExecutor`、硬编码Agent逻辑

---

## 📐 新架构设计

### 1. 统一Agent管理系统

```python
# app/services/agents/unified_agent_manager.py

class AgentRegistry:
    """Agent注册表 - 单一真相来源"""
    
    _agents: Dict[str, Type[BaseAgent]] = {}
    
    @classmethod
    def register(cls, name: str, agent_class: Type[BaseAgent]):
        """注册Agent"""
        cls._agents[name] = agent_class
    
    @classmethod
    def get(cls, name: str) -> BaseAgent:
        """获取Agent实例"""
        if name not in cls._agents:
            raise ValueError(f"Agent '{name}' not registered")
        return cls._agents[name]()
    
    @classmethod
    def list_all(cls) -> List[str]:
        """列出所有已注册Agent"""
        return list(cls._agents.keys())


# 初始化时注册所有Agent
from app.agents.macro_agent import MacroAgent
from app.agents.ta_agent import TAAgent
from app.agents.onchain_agent import OnChainAgent
from app.agents.regime_filter_agent import RegimeFilterAgent
from app.agents.ta_momentum_agent import TAMomentumAgent

AgentRegistry.register('macro', MacroAgent)
AgentRegistry.register('ta', TAAgent)
AgentRegistry.register('onchain', OnChainAgent)
AgentRegistry.register('regime_filter', RegimeFilterAgent)
AgentRegistry.register('ta_momentum', TAMomentumAgent)
```

### 2. 统一Agent执行器

```python
# app/services/agents/unified_executor.py

class UnifiedAgentExecutor:
    """统一的Agent执行器 - 取代RealAgentExecutor和DynamicAgentExecutor"""
    
    def __init__(self):
        self.recorder = AgentExecutionRecorder()
    
    async def execute_agents(
        self,
        agent_names: List[str],  # 从strategy_definition.business_agents读取
        market_data: Dict[str, Any],
        db: AsyncSession,
        user_id: int,
        strategy_execution_id: Optional[int] = None,
        template_execution_batch_id: Optional[uuid.UUID] = None,
    ) -> Tuple[Dict[str, Any], Dict[str, str]]:
        """
        执行指定的Agent列表
        
        Args:
            agent_names: 要执行的Agent名称列表
            
        Returns:
            (agent_outputs, agent_errors)
        """
        agent_outputs = {}
        agent_errors = {}
        
        for agent_name in agent_names:
            try:
                # 从注册表获取Agent
                agent = AgentRegistry.get(agent_name)
                
                # 执行Agent
                start_time = time.time()
                output = await agent.analyze(market_data)
                duration_ms = int((time.time() - start_time) * 1000)
                
                # 保存结果
                agent_outputs[agent_name] = output
                
                # 记录执行(统一方法)
                await self.recorder.record_agent_execution(
                    agent_name=agent_name,
                    output=output,
                    duration_ms=duration_ms,
                    db=db,
                    user_id=user_id,
                    strategy_execution_id=strategy_execution_id,
                    template_execution_batch_id=template_execution_batch_id,
                )
                
                logger.info(f"✅ {agent_name} 执行成功")
                
            except Exception as e:
                logger.error(f"❌ {agent_name} 执行失败: {e}")
                agent_errors[agent_name] = str(e)
                continue
        
        return agent_outputs, agent_errors
```

### 3. 容错的数据采集层

```python
# app/services/data_collectors/resilient_manager.py

class ResilientDataCollectionManager:
    """容错的数据采集管理器"""
    
    def __init__(self):
        self.cache = DataCache()  # Redis or in-memory
        self.mock_generator = MockDataGenerator()
        self.collectors = {
            'binance': BinanceCollector(),
            'fred': FREDCollector(),
            'alternative': AlternativeMeCollector(),
        }
    
    async def collect_all(
        self, 
        use_cache: bool = True,
        fallback_to_mock: bool = True
    ) -> Dict[str, Any]:
        """
        采集所有数据,支持:
        1. 缓存机制
        2. 降级为mock数据
        3. 部分失败继续执行
        """
        data = {}
        
        for source_name, collector in self.collectors.items():
            try:
                # 尝试从缓存读取
                if use_cache:
                    cached = await self.cache.get(source_name)
                    if cached and self._is_cache_valid(cached):
                        logger.info(f"📦 使用缓存数据: {source_name}")
                        data[source_name] = cached
                        continue
                
                # 采集实时数据
                fresh_data = await collector.collect()
                data[source_name] = fresh_data
                
                # 更新缓存
                await self.cache.set(source_name, fresh_data, ttl=300)  # 5分钟
                logger.info(f"✅ 实时数据采集成功: {source_name}")
                
            except Exception as e:
                logger.warning(f"⚠️ {source_name} 采集失败: {e}")
                
                # 尝试使用缓存
                cached = await self.cache.get(source_name)
                if cached:
                    logger.info(f"📦 使用过期缓存: {source_name}")
                    data[source_name] = cached
                    continue
                
                # 降级为mock数据
                if fallback_to_mock:
                    mock_data = self.mock_generator.generate(source_name)
                    logger.warning(f"🎭 使用Mock数据: {source_name}")
                    data[source_name] = mock_data
                else:
                    # 不使用mock,但不影响其他数据源
                    data[source_name] = None
        
        return data
    
    def _is_cache_valid(self, cached_data: dict) -> bool:
        """检查缓存是否有效(不超过15分钟)"""
        timestamp = cached_data.get('_cached_at', 0)
        return (time.time() - timestamp) < 900
```

### 4. 简化的调度器

```python
# app/services/strategy/simplified_scheduler.py

class SimplifiedStrategyScheduler:
    """简化的策略调度器"""
    
    def __init__(self):
        self.data_manager = ResilientDataCollectionManager()
        self.agent_executor = UnifiedAgentExecutor()
        self.orchestrator = StrategyOrchestrator()
    
    async def batch_execute_by_template(self, definition_id: int):
        """按策略模板批量执行"""
        async with AsyncSessionLocal() as db:
            # 1. 获取策略模板和实例
            portfolios = await self._get_active_portfolios(db, definition_id)
            if not portfolios:
                return
            
            definition = portfolios[0].strategy_definition
            batch_id = uuid.uuid4()
            
            logger.info(f"执行策略模板: {definition.display_name}")
            logger.info(f"业务Agents: {definition.business_agents}")
            
            # 2. 采集市场数据(容错)
            try:
                market_data = await self.data_manager.collect_all(
                    use_cache=True,
                    fallback_to_mock=True  # ✅ 失败时使用mock数据
                )
            except Exception as e:
                logger.error(f"数据采集完全失败: {e}")
                return
            
            # 3. 执行Agent分析(统一执行器)
            if not definition.business_agents:
                logger.warning(f"策略 {definition.name} 没有配置business_agents,跳过")
                return
            
            agent_outputs, agent_errors = await self.agent_executor.execute_agents(
                agent_names=definition.business_agents,  # ✅ 完全配置驱动
                market_data=market_data,
                db=db,
                user_id=portfolios[0].user_id,
                template_execution_batch_id=batch_id,
            )
            
            if not agent_outputs:
                logger.error(f"所有Agent执行失败,跳过决策")
                return
            
            # 4. 为每个实例执行决策和交易
            for portfolio in portfolios:
                try:
                    await self.orchestrator.execute_strategy(
                        db=db,
                        user_id=portfolio.user_id,
                        portfolio_id=str(portfolio.id),
                        market_data=market_data,
                        agent_outputs=agent_outputs,
                        template_execution_batch_id=batch_id,
                    )
                except Exception as e:
                    logger.error(f"实例 {portfolio.instance_name} 执行失败: {e}")
                    continue
```

---

## 🚀 重构实施计划

### Phase 1: 紧急修复 (30分钟)
**目标**: 让系统立即可用

1. **添加数据采集容错**
   - 修改`DataCollectionManager.collect_all()`添加try-catch
   - Binance失败时使用mock数据
   - 不阻断其他数据源

2. **临时Mock数据生成器**
```python
# app/services/data_collectors/mock_generator.py
class MockDataGenerator:
    def generate_binance_data(self):
        return {
            'btc_price': 91000.0 + random.uniform(-1000, 1000),
            'btc_24h_change': random.uniform(-5, 5),
            # ...
        }
```

### Phase 2: 统一Agent管理 (2小时)
**目标**: 一套Agent执行逻辑

1. **创建AgentRegistry** ✅
2. **创建UnifiedAgentExecutor** ✅
3. **注册所有Agent** ✅
4. **统一执行记录方法**
5. **更新Scheduler使用新执行器**
6. **废弃RealAgentExecutor**

### Phase 3: 数据层重构 (3小时)
**目标**: 高可用数据采集

1. **实现ResilientDataCollectionManager**
2. **添加Redis缓存支持**
3. **完善MockDataGenerator**
4. **添加数据健康检查**

### Phase 4: 测试与验证 (2小时)
1. **单元测试**: 每个组件独立测试
2. **集成测试**: 完整流程测试
3. **故障模拟**: 模拟数据源失败
4. **性能测试**: 确保不降低性能

---

## 🔧 具体问题解决

### 问题1: 网络连接失败
**临时方案**:
```python
# 检查网络环境
export http_proxy="http://127.0.0.1:7890"
export https_proxy="http://127.0.0.1:7890"
```

**长期方案**:
- 数据采集降级为mock
- 添加proxy配置支持
- 使用备用数据源

### 问题2: UI展示旧Agent
**原因**: 前端从数据库读取`agent_executions`,但调度器未执行新Agent

**解决**:
1. 修复调度器执行(已完成)
2. 确保Agent执行被正确记录
3. 前端轮询最新数据

### 问题3: 调度器无法执行
**原因**: 数据采集失败 → RuntimeError → 调度器中断

**解决**:
1. 添加try-catch包裹数据采集
2. 失败时使用mock数据
3. 记录警告但继续执行

---

## 📋 重构检查清单

### 立即执行(紧急修复)
- [ ] 添加数据采集容错
- [ ] 实现MockDataGenerator
- [ ] 测试网络失败场景
- [ ] 验证策略能自动执行

### 短期(统一Agent)
- [ ] 创建AgentRegistry
- [ ] 创建UnifiedAgentExecutor
- [ ] 废弃RealAgentExecutor
- [ ] 统一执行记录
- [ ] 集成测试

### 中期(数据层)
- [ ] 实现ResilientDataCollectionManager
- [ ] 添加Redis缓存
- [ ] 完善Mock数据
- [ ] 添加健康检查
- [ ] 性能优化

### 长期(监控告警)
- [ ] 添加Prometheus metrics
- [ ] 数据源健康监控
- [ ] Agent执行失败告警
- [ ] 系统可用性监控

---

## 🎓 架构对比

### Before (现状)
```
调度器
  ↓ (硬依赖实时数据)
数据采集失败 → 💥 系统崩溃
  ↓
Agent执行器(两套,逻辑不一致)
  ↓
决策 → 交易
```

**问题**:
- ❌ 单点故障
- ❌ 硬编码逻辑
- ❌ 无容错机制

### After (重构后)
```
调度器
  ↓
数据采集(容错)
  ├→ 实时数据 ✅
  ├→ 缓存数据 📦
  └→ Mock数据 🎭 (降级)
  ↓
统一Agent执行器(配置驱动)
  ↓ business_agents: ['regime_filter', 'ta_momentum']
执行指定Agent ✅
  ↓
决策 → 交易
```

**优势**:
- ✅ 高可用(降级机制)
- ✅ 配置驱动
- ✅ 统一抽象
- ✅ 易维护

---

## ❓ 需要你回答的问题

1. **网络环境**: 
   - 是否需要配置代理才能访问Binance?
   - 可以临时使用mock数据吗?

2. **优先级**:
   - 紧急修复(30分钟,立即可用) → 然后慢慢重构?
   - 还是直接彻底重构(1天,彻底解决)?

3. **数据策略**:
   - A) 必须实时数据,失败就不执行
   - B) 优先实时,失败降级mock
   - C) 使用缓存,最多容忍15分钟延迟

4. **Agent执行记录**:
   - 现在数据库里有旧Agent的记录,需要清理吗?
   - 还是保留历史,只关注新记录?

请告诉我你的选择,我会立即执行对应方案!

