# 启动命令速查

## 🚀 一键启动/停止（推荐）

### 启动前后端服务
```bash
cd /Users/uniteyoo/Documents/AutoMoney
./start.sh
```

### 停止前后端服务
```bash
./stop.sh
```
**功能：**
- 停止所有前后端进程
- 自动清理端口8000和3010
- 强制杀死所有子进程

### 强制清理端口
```bash
./kill-ports.sh
```
**用途：**
- 当stop.sh无法完全清理时使用
- 显示占用端口的进程详情
- 强制杀死所有相关进程
- 二次检查确保端口释放

### 查看服务状态
```bash
./status.sh
```

## ✅ 快速参考

| 命令 | 说明 |
|------|------|
| `./start.sh` | 同时启动前后端服务 |
| `./stop.sh` | 同时停止前后端服务（含端口清理） |
| `./kill-ports.sh` | 强制清理8000和3010端口 |
| `./stop.sh --clean-logs` | 停止服务并清理日志 |
| `./status.sh` | 查看服务运行状态 |
| `tail -f .pids/backend.log` | 查看后端实时日志 |
| `tail -f .pids/frontend.log` | 查看前端实时日志 |
| `./stop.sh && ./start.sh` | 重启所有服务 |

## 📋 访问地址

启动后访问：
- **前端**: http://localhost:3010
- **后端**: http://localhost:8000  
- **API文档**: http://localhost:8000/docs

## 🔍 故障排除

### 端口被占用

**方案1：使用stop.sh（推荐）**
```bash
./stop.sh
./start.sh
```

**方案2：强制清理端口**
```bash
./kill-ports.sh
./start.sh
```

**方案3：手动清理单个端口**
```bash
# 清理后端端口
lsof -ti:8000 | xargs kill -9

# 清理前端端口
lsof -ti:3010 | xargs kill -9
```

### 检查端口占用

```bash
# 查看8000端口
lsof -i:8000

# 查看3010端口
lsof -i:3010

# 查看所有占用端口的进程
lsof -i:8000 -i:3010
```

### 服务无法启动

```bash
# 1. 完全清理
./kill-ports.sh

# 2. 查看日志
tail -50 .pids/backend.log
tail -50 .pids/frontend.log

# 3. 重新启动
./start.sh
```

## 🛠️ 高级用法

### 只清理后端
```bash
lsof -ti:8000 | xargs kill -9
```

### 只清理前端
```bash
lsof -ti:3010 | xargs kill -9
```

### 查看进程树
```bash
# 查看所有Python进程
ps aux | grep python

# 查看所有Node进程
ps aux | grep node

# 查看所有uvicorn进程
ps aux | grep uvicorn
```

### 监控端口
```bash
# 持续监控8000端口
watch -n 1 'lsof -i:8000'

# 持续监控3010端口
watch -n 1 'lsof -i:3010'
```

## 💡 推荐工作流

### 日常启动
```bash
./start.sh          # 一键启动
```

### 遇到端口占用
```bash
./kill-ports.sh     # 强制清理
./start.sh          # 重新启动
```

### 完全重启
```bash
./stop.sh && ./start.sh
```

### 清理后重启
```bash
./stop.sh --clean-logs && ./start.sh
```

## 📝 脚本说明

| 脚本 | 功能 | 适用场景 |
|------|------|---------|
| `start.sh` | 启动服务 | 日常启动 |
| `stop.sh` | 停止服务 | 正常停止 |
| `kill-ports.sh` | 强制清理端口 | 端口被占用 |
| `status.sh` | 查看状态 | 检查服务 |

---

最后更新: 2025-11-05
